# Matcap Library (Optional)

Drop optional matcap textures here for future sculptures. Current build does not require them but the directory stays ready for
expansion without introducing network dependencies.
